# URL del backend FastAPI (sin barra final).
# Desarrollo local típico: http://127.0.0.1:3333
# Mismo origen que la SPA (Nginx hace proxy a :3333): deja VITE_API_BASE_URL vacío
#   VITE_API_BASE_URL=
# O, si enrutas bajo un prefijo: VITE_API_BASE_URL=/api
VITE_API_BASE_URL=http://127.0.0.1:3333
